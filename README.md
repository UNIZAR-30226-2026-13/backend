# backend

## How to run on a container

```sh
npm run docker:build		# Build image
npm run compose:build		# Build compose
npm run compose:up			# Launch image with compose

npm run compose:down		# Stop the services
```

